"""Approved if-then routines — the only way KIKI acts without being asked.

A routine pairs one trigger metric (battery, disk) with one stored tool call.
Creation always shows the full recipe on a confirmation card; the engine later
fires exactly that call, through the normal executor and audit path, and never
anything a human has not seen word for word.
"""
